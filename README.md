# FQLdt Core
Dependently-Typed FormDB Query Language.

## Setup
1. Ensure `just` and `podman` are installed.
2. Run `just check` to verify Lean 4 proofs.

## Cross-Platform Support
For shells other than bash, see `scripts/bootstrap_all.sh`.
